helmDefaults:
  createNamespace: true

releases:
  - name: feedpaper
    namespace: feedpaper
    chart: oci://ghcr.io/rarosalion/charts/feedpaper
    version: "0.1.1"
    values:
      - ./helm/feedpaper/values.yaml
